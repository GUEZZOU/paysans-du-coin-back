# On ajoute ici les dossiers et fichiers à ignorer lors du push sur github.
# Lorsque c'est un dossier on termine par un "/"

node_modules/
.env // Fichier contenant les variables d'environnement (clés API, etc.)